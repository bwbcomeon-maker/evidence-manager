-- ============================================================
-- 双表合一后删除已废弃的 auth_user 表
-- 前置：V10 已将业务表用户外键全部改为 sys_user.id，无表再引用 auth_user。
-- ============================================================

DROP TABLE IF EXISTS auth_user;
