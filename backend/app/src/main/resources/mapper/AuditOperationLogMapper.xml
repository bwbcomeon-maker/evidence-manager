<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bwbcomeon.evidence.mapper.AuditOperationLogMapper">

    <resultMap id="BaseResultMap" type="com.bwbcomeon.evidence.entity.AuditOperationLog">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="actor_user_id" property="actorUserId" jdbcType="OTHER" typeHandler="org.apache.ibatis.type.UUIDTypeHandler"/>
        <result column="action" property="action" jdbcType="VARCHAR"/>
        <result column="target_type" property="targetType" jdbcType="VARCHAR"/>
        <result column="target_id" property="targetId" jdbcType="VARCHAR"/>
        <result column="detail" property="detail" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, actor_user_id, action, target_type, target_id, detail, created_at
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM audit_operation_log
        WHERE id = #{id}
    </select>

    <select id="selectByActorUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM audit_operation_log
        WHERE actor_user_id = #{actorUserId}::uuid
        ORDER BY created_at DESC
    </select>

    <select id="selectByTarget" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM audit_operation_log
        WHERE target_type = #{targetType}
        AND target_id = #{targetId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByAction" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM audit_operation_log
        WHERE action = #{action}
        ORDER BY created_at DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM audit_operation_log
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.bwbcomeon.evidence.entity.AuditOperationLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_operation_log (actor_user_id, action, target_type, target_id, detail, created_at)
        VALUES (
            #{actorUserId}::uuid,
            #{action},
            #{targetType},
            #{targetId},
            #{detail}::jsonb,
            COALESCE(#{createdAt}, CURRENT_TIMESTAMP)
        )
    </insert>

</mapper>
